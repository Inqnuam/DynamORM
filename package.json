{
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.130.0",
    "@aws-sdk/lib-dynamodb": "^3.130.0",
    "@aws-sdk/util-dynamodb": "^3.130.0",
    "@aws/dynamodb-expressions": "^0.7.3"
  },
  "name": "dynamodm",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./dist/index.js",
    "build": "tsc -p ./tsconfig.json",
    "launch": "npm run dist && npm run start"
  },
  "author": "Inqnuam",
  "license": "MIT",
  "description": "ODM for Amazon DynamoDB",
  "devDependencies": {
    "@types/node": "^18.0.4"
  }
}
